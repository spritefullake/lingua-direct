import React from 'react'
import {
    Image,
    Platform,
    ScrollView,
    StyleSheet,
    Text,
    TouchableOpacity,
    View,
  } from 'react-native';

export default class ImagesScreen extends React.Component{
    render(){
     
        return (
            <View>
                <ScrollView>

                </ScrollView>
            </View>
        )
    }
}